import React from 'react';
import preloader from '../../asseds/images/loading.gif'

let Preloader = (props) => {
    return <div style={{}}>
        <img src={preloader} />
    </div>
}

export default Preloader